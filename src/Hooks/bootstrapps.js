import React from 'react';
import { Button } from 'react-bootstrap';
const Styling = () => {
    return (
        <div>
            <Button bsStyle="primary" bsSize="large">Primary</Button>
        </div>
    );

}

export default Styling;

//leave the below code as it is not required for this example